{
  "name": "symbiont-figma-plugin",
  "version": "0.1.0",
  "description": "Figma plugin to sync PDSA design data from GitHub into Figma pages and components.",
  "main": "dist/code.js",
  "scripts": {
    "build": "npm run build:main && npm run build:ui && npm run build:html",
    "build:main": "esbuild src/code.ts --bundle --outfile=dist/code.js --target=es2017 --format=cjs",
    "build:ui": "esbuild src/ui.ts --bundle --outfile=dist/ui.js --target=es2017 --format=iife",
    "build:html": "cp src/ui.html dist/ui.html"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.67.0",
    "esbuild": "^0.19.12",
    "typescript": "^5.4.3"
  }
}
